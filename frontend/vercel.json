{
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "https://job-finder-backend.vercel.app/api/$1"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
